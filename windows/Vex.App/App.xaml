<Application x:Class="Vex.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Neutral graphite palette, matching the terminal's #1F1F24 surface;
             the terminal surface still applies its own theme. -->
        <Color x:Key="VexBackgroundColor">#FF0E1013</Color>
        <Color x:Key="VexSurfaceColor">#E6191B20</Color>
        <Color x:Key="VexHoverColor">#FF21262C</Color>
        <Color x:Key="VexBorderColor">#FF3A424D</Color>
        <Color x:Key="VexTextColor">#FFE6E8EB</Color>
        <Color x:Key="VexTextDimColor">#FF868D96</Color>
        <Color x:Key="VexAccentColor">#FF8B939E</Color>
        <!-- Neutral outline for the active pane; focus reads as a highlight,
             not a saturated frame. -->
        <Color x:Key="VexFocusBorderColor">#FF4C6B9C</Color>
        <!-- Translucent chrome overlays so the acrylic backdrop shows through. -->
        <Color x:Key="VexSidebarColor">#30111317</Color>
        <Color x:Key="VexTabStripColor">#40111317</Color>
        <Color x:Key="VexPaneTitleBarColor">#FF1E2026</Color>
        <Color x:Key="VexPaneTitleBarFocusedColor">#FF262932</Color>
        <Color x:Key="VexTabSelectedColor">#2EFFFFFF</Color>
        <Color x:Key="VexTabHoverColor">#1EFFFFFF</Color>

        <LinearGradientBrush x:Key="VexBackground" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="{DynamicResource VexBackgroundTopColor}" Offset="0"/>
            <GradientStop Color="{DynamicResource VexBackgroundBottomColor}" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="VexSurface" Color="{DynamicResource VexSurfaceColor}"/>
        <SolidColorBrush x:Key="VexHover" Color="{DynamicResource VexHoverColor}"/>
        <SolidColorBrush x:Key="VexBorder" Color="{DynamicResource VexBorderColor}"/>
        <SolidColorBrush x:Key="VexText" Color="{DynamicResource VexTextColor}"/>
        <SolidColorBrush x:Key="VexTextDim" Color="{DynamicResource VexTextDimColor}"/>
        <SolidColorBrush x:Key="VexAccent" Color="{DynamicResource VexAccentColor}"/>
        <SolidColorBrush x:Key="VexFocusBorder" Color="{DynamicResource VexFocusBorderColor}"/>
        <SolidColorBrush x:Key="VexSidebar" Color="{DynamicResource VexSidebarColor}"/>
        <SolidColorBrush x:Key="VexPaneTitleBar" Color="{DynamicResource VexPaneTitleBarColor}"/>
        <SolidColorBrush x:Key="VexPaneTitleBarFocused" Color="{DynamicResource VexPaneTitleBarFocusedColor}"/>
        <SolidColorBrush x:Key="VexTabStrip" Color="{DynamicResource VexTabStripColor}"/>
        <LinearGradientBrush x:Key="VexTabSelected" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="{DynamicResource VexTabSelectedStartColor}" Offset="0"/>
            <GradientStop Color="{DynamicResource VexTabSelectedEndColor}" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="VexTabHover" Color="{DynamicResource VexTabHoverColor}"/>

        <!-- Accent ramp: muted blue and violet used sparingly for highlights
             and gradients over the chrome. Overwritten at runtime by
             ChromePalette from the active terminal theme. -->
        <Color x:Key="VexAccentBlueColor">#FF4C7BB5</Color>
        <Color x:Key="VexAccentPurpleColor">#FF8A6BB5</Color>
        <Color x:Key="VexBackgroundTopColor">#FF0E1013</Color>
        <Color x:Key="VexBackgroundBottomColor">#FF15171B</Color>
        <Color x:Key="VexTabSelectedStartColor">#334C7BB5</Color>
        <Color x:Key="VexTabSelectedEndColor">#338A6BB5</Color>
        <Color x:Key="VexAccentGradientStartColor">#664C7BB5</Color>
        <Color x:Key="VexAccentGradientEndColor">#668A6BB5</Color>
        <SolidColorBrush x:Key="VexAccentBlue" Color="{DynamicResource VexAccentBlueColor}"/>
        <SolidColorBrush x:Key="VexAccentPurple" Color="{DynamicResource VexAccentPurpleColor}"/>
        <LinearGradientBrush x:Key="VexAccentGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="{DynamicResource VexAccentGradientStartColor}" Offset="0"/>
            <GradientStop Color="{DynamicResource VexAccentGradientEndColor}" Offset="1"/>
        </LinearGradientBrush>

        <!-- Variant chrome: overlays, pressed states, drag previews, settings
             surfaces. Defaults mirror the dark variant; ChromePalette rewrites
             every key at startup and on every appearance/theme switch. -->
        <Color x:Key="VexBackdropScrimColor">#66000000</Color>
        <Color x:Key="VexPressFillColor">#FF3A3A42</Color>
        <Color x:Key="VexSidebarToggleBgColor">#14181A1F</Color>
        <Color x:Key="VexSidebarToggleBorderColor">#2630373E</Color>
        <Color x:Key="VexSplitPreviewColor">#40FFFFFF</Color>
        <Color x:Key="VexSplitPreviewBorderColor">#66FFFFFF</Color>
        <Color x:Key="VexPaneExitedColor">#FF6B4B4B</Color>
        <Color x:Key="VexSettingsPanelColor">#F21B1E24</Color>
        <Color x:Key="VexSettingsSidebarColor">#F014171C</Color>
        <Color x:Key="VexCardColor">#FF20242B</Color>
        <Color x:Key="VexCardBorderColor">#FF2A3037</Color>
        <Color x:Key="VexTrackColor">#FF3A414C</Color>
        <Color x:Key="VexNavHoverColor">#FF21262C</Color>
        <Color x:Key="VexNavSelectedColor">#FF2E343D</Color>
        <Color x:Key="VexInputColor">#FF1B1E24</Color>
        <Color x:Key="VexToggleThumbColor">#FFEDEFF3</Color>
        <Color x:Key="VexOnAccentColor">#FFEDEFF2</Color>
        <SolidColorBrush x:Key="VexBackdropScrim" Color="{DynamicResource VexBackdropScrimColor}"/>
        <SolidColorBrush x:Key="VexPressFill" Color="{DynamicResource VexPressFillColor}"/>
        <SolidColorBrush x:Key="VexSidebarToggleBg" Color="{DynamicResource VexSidebarToggleBgColor}"/>
        <SolidColorBrush x:Key="VexSidebarToggleBorder" Color="{DynamicResource VexSidebarToggleBorderColor}"/>
        <SolidColorBrush x:Key="VexSplitPreview" Color="{DynamicResource VexSplitPreviewColor}"/>
        <SolidColorBrush x:Key="VexSplitPreviewBorder" Color="{DynamicResource VexSplitPreviewBorderColor}"/>
        <SolidColorBrush x:Key="VexPaneExited" Color="{DynamicResource VexPaneExitedColor}"/>
        <SolidColorBrush x:Key="VexCard" Color="{DynamicResource VexCardColor}"/>
        <SolidColorBrush x:Key="VexCardBorder" Color="{DynamicResource VexCardBorderColor}"/>
        <SolidColorBrush x:Key="VexTrack" Color="{DynamicResource VexTrackColor}"/>
        <SolidColorBrush x:Key="VexInput" Color="{DynamicResource VexInputColor}"/>
        <SolidColorBrush x:Key="VexToggleThumb" Color="{DynamicResource VexToggleThumbColor}"/>
        <SolidColorBrush x:Key="VexOnAccent" Color="{DynamicResource VexOnAccentColor}"/>

        <!-- Shared hover treatment: a soft fill fades in/out over ~0.12s with
             the same easing on every button so the chrome feels consistent.
             Accent (blue→violet) is reserved for selection and the Deck
             toggle; plain buttons hover neutral. -->
        <Style TargetType="Button" x:Key="GhostButton">
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource VexTextDim}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="hoverFill" Background="{StaticResource VexHover}" CornerRadius="4" Opacity="0"/>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource VexText}"/>
                            </Trigger>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="hoverFill" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.12">
                                            <DoubleAnimation.EasingFunction><QuadraticEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="hoverFill" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.12">
                                            <DoubleAnimation.EasingFunction><QuadraticEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="SidebarToggleButton" BasedOn="{StaticResource GhostButton}">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Margin" Value="4,0,-8,0"/>
            <Setter Property="Background" Value="{StaticResource VexSidebarToggleBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource VexSidebarToggleBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="chrome" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <Grid>
                                <Border x:Name="hoverFill" Background="{StaticResource VexAccentGradient}" CornerRadius="5" Opacity="0"/>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="chrome" Property="BorderBrush" Value="{StaticResource VexAccentBlue}"/>
                                <Setter Property="Foreground" Value="{StaticResource VexText}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="chrome" Property="Background" Value="{StaticResource VexPressFill}"/>
                            </Trigger>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="hoverFill" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.12">
                                            <DoubleAnimation.EasingFunction><QuadraticEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="hoverFill" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.12">
                                            <DoubleAnimation.EasingFunction><QuadraticEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="PaneTitleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource VexText}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Margin" Value="1,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="hoverFill" Background="{StaticResource VexHover}" CornerRadius="4" Opacity="0"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource VexText}"/>
                            </Trigger>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="hoverFill" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.12">
                                            <DoubleAnimation.EasingFunction><QuadraticEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="hoverFill" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.12">
                                            <DoubleAnimation.EasingFunction><QuadraticEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="CloseButton">
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource VexTextDim}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Width" Value="22"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="hoverFill" Background="{StaticResource VexHover}" CornerRadius="4" Opacity="0"/>
                            <Border x:Name="pressFill" Background="{StaticResource VexPressFill}" CornerRadius="4" Opacity="0"/>
                            <ContentPresenter x:Name="content" HorizontalAlignment="Center" VerticalAlignment="Center"
                                              RenderTransformOrigin="0.5,0.5">
                                <ContentPresenter.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </ContentPresenter.RenderTransform>
                            </ContentPresenter>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource VexText}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="pressFill" Property="Opacity" Value="1"/>
                            </Trigger>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="hoverFill" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.12">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="hoverFill" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="WindowControlButton">
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource VexTextDim}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="hoverFill" Background="{StaticResource VexHover}" Opacity="0"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource VexText}"/>
                            </Trigger>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="hoverFill" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.12">
                                            <DoubleAnimation.EasingFunction><QuadraticEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="hoverFill" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.12">
                                            <DoubleAnimation.EasingFunction><QuadraticEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="WindowCloseButton" BasedOn="{StaticResource WindowControlButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="hoverFill" Background="#E81123" Opacity="0"/>
                            <Border x:Name="pressFill" Background="#F1707A" Opacity="0"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="pressFill" Property="Opacity" Value="1"/>
                            </Trigger>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="hoverFill" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.12">
                                            <DoubleAnimation.EasingFunction><QuadraticEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="hoverFill" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.12">
                                            <DoubleAnimation.EasingFunction><QuadraticEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="DeckButton" BasedOn="{StaticResource GhostButton}">
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="0"/>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource VexHover}"/>
            <Setter Property="Width" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Border Background="Transparent"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="true">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border x:Name="thumbBorder" CornerRadius="4" 
                                                        Background="{StaticResource VexHover}" 
                                                        Margin="2,0" 
                                                        Width="6"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="thumbBorder" Property="Background" Value="{StaticResource VexTextDim}"/>
                                                        <Setter TargetName="thumbBorder" Property="Margin" Value="0,0"/>
                                                        <Setter TargetName="thumbBorder" Property="Width" Value="10"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="10"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid>
                                    <Border Background="Transparent"/>
                                    <Track x:Name="PART_Track" IsDirectionReversed="False">
                                        <Track.Thumb>
                                            <Thumb>
                                                <Thumb.Template>
                                                    <ControlTemplate TargetType="Thumb">
                                                        <Border x:Name="thumbBorder" CornerRadius="4" 
                                                                Background="{StaticResource VexHover}" 
                                                                Margin="0,2" 
                                                                Height="6"/>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="thumbBorder" Property="Background" Value="{StaticResource VexTextDim}"/>
                                                                <Setter TargetName="thumbBorder" Property="Margin" Value="0,0"/>
                                                                <Setter TargetName="thumbBorder" Property="Height" Value="10"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Thumb.Template>
                                            </Thumb>
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox" x:Key="DarkComboBox">
            <Setter Property="Foreground" Value="{StaticResource VexText}"/>
            <Setter Property="Background" Value="{StaticResource VexSurface}"/>
            <Setter Property="BorderBrush" Value="{StaticResource VexBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          BorderBrush="{TemplateBinding BorderBrush}" 
                                          Background="{TemplateBinding Background}" 
                                          Foreground="{TemplateBinding Foreground}" 
                                          BorderThickness="{TemplateBinding BorderThickness}" 
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="Arrow" Grid.Column="1" Fill="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{StaticResource VexHover}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="10,6,24,6" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="{StaticResource VexSurface}" BorderThickness="1" BorderBrush="{StaticResource VexBorder}" CornerRadius="4"/>
                                    <ScrollViewer Margin="0,4" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem" x:Key="DarkComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource VexText}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource VexHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource VexAccent}"/>
                                <Setter Property="Foreground" Value="{StaticResource VexOnAccent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="CheckBox" x:Key="DarkCheckBox">
            <Setter Property="Foreground" Value="{StaticResource VexText}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource VexBorder}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Grid Width="16" Height="16" VerticalAlignment="Center">
                                <Border x:Name="box" Background="{StaticResource VexSurface}"
                                        BorderBrush="{StaticResource VexBorder}" BorderThickness="1" CornerRadius="3"/>
                                <Path x:Name="check" Data="M 3,8 L 6.5,11.5 L 13,4"
                                      Stroke="{StaticResource VexText}" StrokeThickness="1.6"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                      Visibility="Collapsed"/>
                            </Grid>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource VexAccent}"/>
                                <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource VexAccent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ContextMenu" x:Key="DarkContextMenu">
            <Setter Property="Background" Value="{StaticResource VexSurface}"/>
            <Setter Property="Foreground" Value="{StaticResource VexText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource VexBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer>
                                <ItemsPresenter Margin="{TemplateBinding Padding}"/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem" x:Key="DarkMenuItem">
            <Setter Property="Foreground" Value="{StaticResource VexText}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter ContentSource="Header" RecognizesAccessKey="True"/>
                                <TextBlock Grid.Column="1" Text="{TemplateBinding InputGestureText}" Margin="16,0,0,0" Foreground="{StaticResource VexTextDim}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource VexHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
